<?xml version="1.0" encoding="utf-8" ?>
<ContentDialog
    x:Class="SimpliPDF.Dialogs.CropDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:SimpliPDF.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Crop page"
    PrimaryButtonText="Apply"
    CloseButtonText="Cancel"
    DefaultButton="Primary"
    PrimaryButtonClick="OnApplyClick"
    mc:Ignorable="d">

    <Grid MinWidth="500" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <HyperlinkButton x:Name="ResetCropButton"
                         Content="Reset crop"
                         Click="OnResetCropClick"
                         Visibility="Collapsed" />

        <!-- Preview + crop overlay -->
        <Border Grid.Row="1"
                MinHeight="380"
                Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="4"
                Padding="8">
            <Grid>
                <ProgressRing x:Name="LoadingRing"
                              IsActive="True"
                              Width="40"
                              Height="40"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center" />
                <controls:CropOverlay x:Name="Crop" Visibility="Collapsed" />
            </Grid>
        </Border>

        <TextBlock x:Name="StatusText"
                   Grid.Row="2"
                   Style="{StaticResource CaptionTextBlockStyle}"
                   Opacity="0.6"
                   Text="Drag to select the area to keep." />
    </Grid>
</ContentDialog>
