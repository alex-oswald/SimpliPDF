<?xml version="1.0" encoding="utf-8" ?>
<ContentDialog
    x:Class="SimplePDF.Dialogs.ScanDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Scan"
    PrimaryButtonText="Scan"
    CloseButtonText="Cancel"
    DefaultButton="Primary"
    IsPrimaryButtonEnabled="False"
    PrimaryButtonClick="OnPrimaryButtonClick">

    <Grid MinWidth="500" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Settings row -->
        <StackPanel Spacing="12">
            <ComboBox x:Name="ScannerCombo"
                      Header="Scanner"
                      HorizontalAlignment="Stretch"
                      PlaceholderText="Searching for scanners..."
                      SelectionChanged="OnScannerSelectionChanged"
                      DisplayMemberPath="Name" />

            <Grid ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <ComboBox x:Name="DpiCombo"
                          Header="Resolution"
                          HorizontalAlignment="Stretch"
                          PlaceholderText="Select scanner first"
                          IsEnabled="False" />

                <ComboBox x:Name="ColorModeCombo"
                          Grid.Column="1"
                          Header="Color mode"
                          HorizontalAlignment="Stretch"
                          PlaceholderText="Select scanner first"
                          IsEnabled="False" />
            </Grid>

            <Button x:Name="PreviewButton"
                    Content="Preview"
                    Click="OnPreviewClick"
                    IsEnabled="False"
                    HorizontalAlignment="Left" />
        </StackPanel>

        <!-- Progress bar -->
        <ProgressBar x:Name="ScanProgress"
                     Grid.Row="1"
                     IsIndeterminate="True"
                     Visibility="Collapsed" />

        <!-- Preview image area -->
        <Border Grid.Row="2"
                MinHeight="300"
                Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="4"
                Padding="8">
            <Grid>
                <!-- Placeholder text -->
                <TextBlock x:Name="PreviewPlaceholder"
                           Text="Click Preview to see a scan preview"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Opacity="0.5"
                           Style="{StaticResource BodyTextBlockStyle}" />

                <!-- Preview image -->
                <Image x:Name="PreviewImage"
                       Stretch="Uniform"
                       Visibility="Collapsed" />
            </Grid>
        </Border>

        <!-- Status text -->
        <TextBlock x:Name="StatusText"
                   Grid.Row="3"
                   Style="{StaticResource CaptionTextBlockStyle}"
                   Opacity="0.6" />
    </Grid>
</ContentDialog>
