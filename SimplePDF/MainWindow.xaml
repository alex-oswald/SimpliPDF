<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="SimplePDF.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:SimplePDF"
    xmlns:models="using:SimplePDF.Models"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="SimplePDF"
    mc:Ignorable="d">
    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Title Bar -->
        <TitleBar x:Name="AppTitleBar" Title="SimplePDF">
            <TitleBar.IconSource>
                <ImageIconSource ImageSource="Assets/AppIcon.ico" />
            </TitleBar.IconSource>
        </TitleBar>

        <!-- Command Bar -->
        <CommandBar Grid.Row="1" DefaultLabelPosition="Right" IsOpen="False">
            <AppBarButton Icon="OpenFile" Label="Open" Click="OnOpenClick"
                          KeyboardAcceleratorTextOverride="Ctrl+O">
                <AppBarButton.KeyboardAccelerators>
                    <KeyboardAccelerator Modifiers="Control" Key="O" />
                </AppBarButton.KeyboardAccelerators>
            </AppBarButton>
            <AppBarButton Icon="Save" Label="Save" Click="OnSaveClick"
                          KeyboardAcceleratorTextOverride="Ctrl+S">
                <AppBarButton.KeyboardAccelerators>
                    <KeyboardAccelerator Modifiers="Control" Key="S" />
                </AppBarButton.KeyboardAccelerators>
            </AppBarButton>
            <AppBarButton Icon="Print" Label="Print" Click="OnPrintClick"
                          KeyboardAcceleratorTextOverride="Ctrl+P">
                <AppBarButton.KeyboardAccelerators>
                    <KeyboardAccelerator Modifiers="Control" Key="P" />
                </AppBarButton.KeyboardAccelerators>
            </AppBarButton>
            <AppBarSeparator />
            <AppBarButton Icon="Delete" Label="Delete" Click="OnDeleteClick"
                          KeyboardAcceleratorTextOverride="Del">
                <AppBarButton.KeyboardAccelerators>
                    <KeyboardAccelerator Key="Delete" />
                </AppBarButton.KeyboardAccelerators>
            </AppBarButton>
            <AppBarButton Label="Rotate" Click="OnRotateClick"
                          KeyboardAcceleratorTextOverride="Ctrl+R">
                <AppBarButton.Icon>
                    <FontIcon Glyph="&#xE7AD;" />
                </AppBarButton.Icon>
                <AppBarButton.KeyboardAccelerators>
                    <KeyboardAccelerator Modifiers="Control" Key="R" />
                </AppBarButton.KeyboardAccelerators>
            </AppBarButton>
            <AppBarSeparator />
            <AppBarButton Label="Split All" Click="OnSplitClick">
                <AppBarButton.Icon>
                    <FontIcon Glyph="&#xE8A9;" />
                </AppBarButton.Icon>
            </AppBarButton>
            <AppBarButton Label="Extract" Click="OnExtractClick">
                <AppBarButton.Icon>
                    <FontIcon Glyph="&#xE8B5;" />
                </AppBarButton.Icon>
            </AppBarButton>
        </CommandBar>

        <!-- Main content area — this is the drop target -->
        <Grid Grid.Row="2"
              AllowDrop="True"
              DragOver="OnDragOver"
              Drop="OnDrop"
              Background="Transparent">

            <!-- Empty state -->
            <StackPanel x:Name="EmptyState" HorizontalAlignment="Center" VerticalAlignment="Center"
                        Spacing="12">
                <FontIcon Glyph="&#xE8E5;" FontSize="64" Opacity="0.4" HorizontalAlignment="Center" />
                <TextBlock Text="Drop PDF files here or click Open"
                           Style="{StaticResource SubtitleTextBlockStyle}"
                           Opacity="0.6" HorizontalAlignment="Center" />
            </StackPanel>

            <!-- Loading overlay -->
            <ProgressRing IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}"
                          Width="48" Height="48"
                          HorizontalAlignment="Center" VerticalAlignment="Center" />

            <!-- Page thumbnails -->
            <GridView x:Name="PagesGridView"
                      ItemsSource="{x:Bind ViewModel.Pages}"
                      SelectionMode="Extended"
                      CanReorderItems="True"
                      CanDragItems="True"
                      AllowDrop="True"
                      DragOver="OnDragOver"
                      Drop="OnDrop"
                      Padding="12">
                <GridView.ItemTemplate>
                    <DataTemplate x:DataType="models:PdfPageItem">
                        <Grid Width="150" Padding="4" RowSpacing="4"
                              Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                              BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                              BorderThickness="1" CornerRadius="4">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="180" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <Image Source="{x:Bind Thumbnail, Mode=OneWay}"
                                   Stretch="Uniform"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   RenderTransformOrigin="0.5,0.5">
                                <Image.RenderTransform>
                                    <RotateTransform Angle="{x:Bind Rotation, Mode=OneWay}" />
                                </Image.RenderTransform>
                            </Image>

                            <TextBlock Grid.Row="1"
                                       Text="{x:Bind DisplayLabel}"
                                       Style="{StaticResource CaptionTextBlockStyle}"
                                       HorizontalAlignment="Center"
                                       TextTrimming="CharacterEllipsis"
                                       Padding="2,0,2,4"
                                       MaxLines="1" />
                        </Grid>
                    </DataTemplate>
                </GridView.ItemTemplate>
            </GridView>
        </Grid>

        <!-- Status Bar -->
        <Border Grid.Row="3"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="0,1,0,0"
                Padding="16,6">
            <TextBlock Text="{x:Bind ViewModel.StatusText, Mode=OneWay}"
                       Style="{StaticResource CaptionTextBlockStyle}" />
        </Border>
    </Grid>
</Window>
